version: 2
jobs:
  build:
    docker:
      - image: fazenda/r-testing
    steps:
      - checkout
      - run:
          command: |
            cd ./src/r/
            R -e 'devtools::test()'
      - store_artifacts:
          path: man/
          destination: man
