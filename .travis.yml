language: r

cache: packages

before_install:
  cd src/r

r_github_packages:
  - r-lib/covr

install:
  R -e 'install.packages(c("devtools", "covr", "shiny", "DT"))'
  R -e 'devtools::install_deps(dep = T)'

after_success:
  R -e 'covr::codecov()'
  R -e 'covr::coveralls()'
