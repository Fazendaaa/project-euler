sudo: required

services:
  - docker

before_test:
  pwd

script:
  docker run -v /home/appveyor/projects/project-euler/src/r/:/toBeTested -t fazenda/r-testing /bin/sh -c "cd toBeTested && R -e 'devtools::test()'"

after_success:
  R -e 'covr::codecov()'
  R -e 'covr::coveralls()'
